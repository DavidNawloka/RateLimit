[project]
name = "ratelimit"
version = "0.1.0"
description = "Rate limiting API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.1",
    "pytest>=9.0.3",
    "uvicorn>=0.47.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.3",
]
